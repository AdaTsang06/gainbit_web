import React, { PureComponent } from 'react';
import ForgotPassword from '../../pages/user-center/forgot-password';

export default class Forgot extends PureComponent {
  render() {
    return (
      <div style={{ width: '100%' }}>
        <ForgotPassword />
      </div>
    );
  }
}
